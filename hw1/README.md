I used a simple implementation of Model 1, with penalties added for alignments that are far from the diagonal as well as for alignments between words with a high edit distance.  The model is trained with 5 EM iterations, although this value can be changed with the -i command line parameter (the -b and -n parameters work the same as in the starter code).
